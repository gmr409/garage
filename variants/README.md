# Variants

Store retained design alternatives here.

Each variant should identify:
- the baseline version it derives from
- the exact changes introduced
- whether the variant is experimental, retained, or promoted to a milestone
- any reference images and machine-readable prompt/spec files

A variant never replaces the baseline unless explicitly promoted.
